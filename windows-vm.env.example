# Existing libvirt Windows 11 VM containing Autodesk Inventor 2026.
VM_NAME="inventor-win11"

# Optional fixed VM IPv4 address. Leave blank to auto-discover it with virsh.
VM_IP=""

# Inventor bridge port inside the Windows VM.
INVENTOR_BRIDGE_PORT="8765"
